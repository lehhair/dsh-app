{
  "name": "dsh-app",
  "version": "0.3.7",
  "private": true,
  "type": "module",
  "description": "dsh app shell (Tauri 2): embedded dsh runtime via bundled official Node, plus remote dsh node support",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "preview:web": "npm run build:web && node scripts/serve-web.mjs",
    "bundle:resources": "node scripts/bundle-resources.mjs",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:bundled": "npm run bundle:resources && npm run tauri -- build",
    "build:external": "node scripts/bundle-resources.mjs --external && npm run tauri -- build"
  },
  "dependencies": {
    "npm": "^12.0.2"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/cli": "^2.11.0",
    "esbuild": "^0.25.0"
  }
}
