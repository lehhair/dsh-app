# Embedded-run overlay.
#
# Empty by design: the embedded dsh loads the same plugins as a terminal
# `dsh web`, including remote-gateway when the user has it. If that
# gateway's fixed 8443 port is already taken by another gateway, the plugin
# fails to bind and dsh reports the failure — that is the user's environment
# and their choice, not something this shell should silently override.
